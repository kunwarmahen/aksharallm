.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# artifacts — large, regenerable. Leading "/" anchors these to the repo root so they do
# NOT also match the source package aksharallm/data/ (a bare "data/" matches any depth).
/data/
/checkpoints/
/logs/
/runs/
/wandb/
/out/
*.bin
*.pt
*.safetensors
# local-only working files (not part of the presentable repo)
AGENTS.md
PLAN.md
.claude/
train_*.log
# the local training schedule (your evenings, not the project's)
/schedule.json
/learning/
